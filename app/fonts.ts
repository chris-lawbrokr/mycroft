import localFont from "next/font/local";

// Self-hosted display font for headlines (e.g. "The People's Attorney").
export const rhymesDisplay = localFont({
  src: "../public/fonts/Rhymes Display Medium.woff2",
  display: "swap",
});
